var dirName = "";
    
function load(){
    //look at all the directories inside dirName
        //make a new button that links to the new dir
            //onclick events - clearing the page
            //change the dirName to itself --> append /newdir
            //call load again
    //look at all the files inside dirName
        //when click go to new dir    
}

function toParent(){
    //goes back a directory
    var dirNameLength = dirName.length;
    for(int i = dirNameLength, i > 0 ; i --)
    //look at dirName
    //remove until the next "/"
}

function checkChar
